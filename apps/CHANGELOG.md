# 更新日志

本文件记录 Fun Harness AI 研发流程插件的所有重要变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.0] - 2026-05-20

### 新增
- **自动轮询远程任务**：在「高级设置」中新增「自动轮询远程任务」配置，填写轮询间隔（秒）与拉取脚本（固定放在主目录 `script/` 下，推荐 Node 脚本 `pullTask.js`，可一键创建示例）。脚本约定把拉取到的任务清单**打印到 stdout**；在任意 worktree 的**子面板**上即可「▶ 开启自动轮询远程任务」：当前 worktree 立即拉取一次、随后按间隔轮询，插件读取 stdout 后**仅当内容非空且与现有 `todo.md` 不同**才覆盖该 worktree 下的 `todo.md`（拉取为空或无变化时不覆盖）。开关具有**排它性**——同一时间仅允许一个 worktree 开启；在其它 worktree 仍在运行时点击开启会提示「某某 worktree 已开启，请移步或先关闭」。跨独立窗口通过主目录 `.harness/auto-poll-lock.json` 协调，窗口重载后自动恢复本 worktree 的轮询。新增「拉取到新任务后自动唤起 AI 执行」开关：开启后每当 `todo.md` 拉取到新内容，会把任务自动派发给当前迭代任务卡片选择的 AI 执行器（选 Claude Code CLI 可全自动在终端运行；选 Claude Code 面板会通过深链 `vscode://anthropic.claude-code/open?prompt=...` 自动唤起面板并**预填提示词**（暂不自动发送，未自动发送时按回车即可），完整提示词同时复制到剪贴板）。
- **Claude Code 面板自动唤起并预填**：「Claude Code (面板)」执行器改为优先直接调用其命令 `claude-vscode.primaryEditor.open(session, prompt)`（即深链 `vscode://anthropic.claude-code/open?prompt=...` 内部实际调用的命令），把完整提示词作为参数传入——无 URL 长度上限、无百分号编码问题；命令不可用时回退到深链（由系统 opener 打开以避免 URI 重编码），再不行回退到「打开面板 + 复制剪贴板」。该改进对所有走面板执行器的派发（阶段 Agent、开发子任务、自动轮询）均生效。注：Claude Code 未对外提供"自动发送"能力（深链/命令只能预填输入框），因此预填后仍需按回车发送。
- **自定义按钮**：在「高级设置」中新增「自定义按钮」配置。脚本统一维护在主目录的 `script/` 目录下（如 `deploy.sh`）；为按钮填写名称并从下拉列表中**选择**一个脚本即可，支持新增/删除多个。提供「打开 script 目录」（自动创建并在系统中打开，引导用户把脚本放入该目录）与「刷新脚本列表」；目录为空时给出创建指引。配置后按钮显示在主面板与子面板的任务卡片「旁路操作」区。点击时先 cd 到该任务的 worktree 迭代目录，再运行主目录 `script/` 下所选脚本（一套脚本对所有迭代生效），插件按当前操作系统自动拼接执行命令（`.sh` 用 bash、Windows `.ps1` 用 powershell、其余按系统关联执行）。已存在的子 worktree 面板在窗口重载后同步显示新按钮。

## [1.0.0] - 2026-05-18
- 修复了一些bug

## [0.0.9] - 2026-05-14

### 新增
- **快捷模式**：创建迭代任务时可勾选"快捷模式"，跳过需求/设计/测试用例/任务拆解，直接进入开发阶段，开发 Agent 仅依据任务描述与项目结构上下文工作。
- **统一"启动服务"按钮**：合并原"启动前端 / 启动后端"为单一入口，扫描迭代目录下的 frontend / backend 子仓逐个启动；按钮在 worktree 子面板任意阶段常驻可见。
- **fun_harness_start 启动脚本**：未配置启动命令时派发 AI Agent 生成 `fun_harness_start.sh` / `.ps1`；启动脚本自动清理占用端口与残留进程，并针对 Java 多模块工程识别可启动模块。
- **"提交代码"按钮**：将原"手动提交代码"升级为推送迭代分支并合并到远程基线（保留 worktree 与迭代分支，便于继续迭代）。

### 修复
- **合并代码丢失风险**：重写"完成任务并合并"为三阶段流程（备份推送 → 合并校验 → 安全清理），任一步失败立即中止且不删除任何本地/远程内容；推送后通过 SHA 与祖先校验确认远程基线确实包含本次提交。
- **任务状态不同步**：在 worktree 子面板完成合并后，任务状态会回写主项目根的状态文件；主面板保存时不再把已推进的阶段回退。
- **误报"所有编码任务已完成"**：并发执行场景下，仍有子任务运行时不再提前提示全部完成。
- **Windows 启动脚本调用失败**：改用系统自带的 `powershell` 调用启动脚本，不再依赖未必安装的 `pwsh`（PowerShell 7+）。

### 变更
- 重写 README，按插件实际能力组织内容。
- 开源许可证由 MIT 切换为 PolyForm Noncommercial 1.0.0（仅限非商业用途）。

## [0.0.7] - 2026-05-11

### 变更
- 初始化代码仓库功能优化。

## [0.0.6]

- 首个公开发布版本。
