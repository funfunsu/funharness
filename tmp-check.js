const v={}; const TODO_STATE_KEY='workspaceTodoState.v1'; const TODO_SOURCE_PANEL='master'; const TODO_INITIAL_TODOS=[]; let todoState=null; function loadTodoState(){ const state={}; const cached=state[TODO_STATE_KEY]; const fallback={}; if(!cached||typeof cached!=='object'){return fallback;} return {todos:Array.isArray(TODO_INITIAL_TODOS)?TODO_INITIAL_TODOS:(Array.isArray(cached.todos)?cached.todos:[]),loading:cached.loading===true}; } todoState=loadTodoState();
